---
title: "Gemini"
description: "Añade firmas electrónicas con validez legal a cualquier aplicación basada en Gemini usando function calling, la CLI de Gemini o Gemini Code Assist."
---

Firma te permite añadir firmas electrónicas con validez legal a cualquier cosa construida sobre Gemini. Utiliza el function calling de la API de Gemini para que tus agentes de IA envíen solicitudes de firma bajo demanda, conecta los servidores MCP de Firma a la CLI de Gemini para que la IA genere código de integración preciso, o configura webhooks para hacer un seguimiento de las finalizaciones. Esta guía cubre las tres opciones.

## Requisitos previos

- Una [cuenta de Firma](https://app.firma.dev) con una clave de API
- Una cuenta de Google AI Studio con una clave de API de Gemini, o Gemini Code Assist instalado en tu IDE
- Al menos una plantilla de Firma con campos de firma configurados

<Note>
  Firma utiliza la clave de API sin procesar como valor del encabezado `Authorization`; no le antepongas `Bearer`. Esto difiere de la propia API de Gemini, que usa `x-goog-api-key`.
</Note>

## Primeros pasos

<Tabs>

<Tab title="Function calling">

Function calling permite que un modelo Gemini decida cuándo llamar a tu código. Declaras una función que el modelo puede llamar y, cuando el usuario le pide enviar un contrato, Gemini devuelve una llamada de herramienta estructurada que tu aplicación ejecuta contra la API de Firma.

Esta ruta es adecuada para agentes de IA, chatbots y cualquier aplicación en la que una petición del usuario en lenguaje natural deba convertirse en una solicitud de firma.

### Paso 1: guarda tus claves de API

Mantén tanto tu clave de API de Gemini como tu clave de API de Firma en variables de entorno. Nunca las pongas en el código del frontend.

```bash
GEMINI_API_KEY=your_gemini_key
FIRMA_API_KEY=your_firma_key
```

### Paso 2: declara una función y gestiona las llamadas a herramientas

Declara la función con la forma que Gemini espera y luego ejecuta un bucle de chat que gestione la llamada a la herramienta llamando al endpoint `create-and-send` de Firma.

```javascript
import { GoogleGenAI, Type } from "@google/genai";

const FIRMA_API = "https://api.firma.dev/functions/v1/signing-request-api";

const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

const sendSigningRequestDeclaration = {
  name: "send_signing_request",
  description:
    "Send a document for e-signature via Firma. Uses a pre-configured template and one signer.",
  parameters: {
    type: Type.OBJECT,
    properties: {
      name: {
        type: Type.STRING,
        description: "A descriptive name for the signing request.",
      },
      template_id: {
        type: Type.STRING,
        description: "The ID of the Firma template to send.",
      },
      signer_email: { type: Type.STRING },
      signer_first_name: { type: Type.STRING },
      signer_last_name: { type: Type.STRING },
    },
    required: [
      "name",
      "template_id",
      "signer_email",
      "signer_first_name",
      "signer_last_name",
    ],
  },
};

async function sendSigningRequest(args) {
  const response = await fetch(
    `${FIRMA_API}/signing-requests/create-and-send`,
    {
      method: "POST",
      headers: {
        Authorization: process.env.FIRMA_API_KEY,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        name: args.name,
        template_id: args.template_id,
        recipients: [
          {
            first_name: args.signer_first_name,
            last_name: args.signer_last_name,
            email: args.signer_email,
            designation: "Signer",
            order: 1,
          },
        ],
      }),
    }
  );

  const data = await response.json();
  if (!response.ok) {
    return { error: data };
  }

  return {
    signing_request_id: data.id,
    status: "sent",
    signing_link: data.first_signer?.signing_link,
  };
}

export async function handleUserMessage(userText) {
  const result = await ai.models.generateContent({
    model: "gemini-2.5-flash",
    contents: userText,
    config: {
      tools: [{ functionDeclarations: [sendSigningRequestDeclaration] }],
    },
  });

  const call = result.functionCalls?.[0];
  if (!call) {
    return { text: result.text };
  }

  const toolResult = await sendSigningRequest(call.args);

  const followUp = await ai.models.generateContent({
    model: "gemini-2.5-flash",
    contents: [
      { role: "user", parts: [{ text: userText }] },
      { role: "model", parts: [{ functionCall: call }] },
      {
        role: "user",
        parts: [
          {
            functionResponse: {
              name: call.name,
              response: toolResult,
            },
          },
        ],
      },
    ],
  });

  return { text: followUp.text, result: toolResult };
}
```

<Note>
  El endpoint `create-and-send` crea la solicitud de firma y la envía a los destinatarios en una sola llamada a la API. Si necesitas que el modelo elabore una solicitud para revisar antes de enviarla, usa `POST /signing-requests` para crear un borrador y luego `POST /signing-requests/{id}/send` una vez aprobada.
</Note>

### Paso 3: conéctalo a tu aplicación

Llama a `handleUserMessage` desde tu backend cada vez que el usuario envíe un mensaje de chat. Gemini decide por sí mismo cuándo la petición justifica un flujo de firma:

```javascript
// e.g. an Express handler
app.post("/chat", async (req, res) => {
  const reply = await handleUserMessage(req.body.message);
  res.json(reply);
});
```

Un mensaje del usuario como "Envía el contrato de consultoría a alice@acme.com" activará la llamada a la función. El modelo devuelve el `functionCall` estructurado, tu código lo ejecuta contra Firma, y el turno de seguimiento permite a Gemini resumir el resultado al usuario.

### Alternativa en Python

El mismo flujo con el SDK de Python:

```python
import os
import requests
from google import genai
from google.genai import types

FIRMA_API = "https://api.firma.dev/functions/v1/signing-request-api"
client = genai.Client(api_key=os.environ["GEMINI_API_KEY"])

send_signing_request = {
    "name": "send_signing_request",
    "description": "Send a document for e-signature via Firma.",
    "parameters": {
        "type": "object",
        "properties": {
            "name": {"type": "string", "description": "A descriptive name for the signing request."},
            "template_id": {"type": "string"},
            "signer_email": {"type": "string"},
            "signer_first_name": {"type": "string"},
            "signer_last_name": {"type": "string"},
        },
        "required": [
            "name",
            "template_id",
            "signer_email",
            "signer_first_name",
            "signer_last_name",
        ],
    },
}

def execute(args):
    r = requests.post(
        f"{FIRMA_API}/signing-requests/create-and-send",
        headers={
            "Authorization": os.environ["FIRMA_API_KEY"],
            "Content-Type": "application/json",
        },
        json={
            "name": args["name"],
            "template_id": args["template_id"],
            "recipients": [
                {
                    "first_name": args["signer_first_name"],
                    "last_name": args["signer_last_name"],
                    "email": args["signer_email"],
                    "designation": "Signer",
                    "order": 1,
                }
            ],
        },
    )
    return r.json()

result = client.models.generate_content(
    model="gemini-2.5-flash",
    contents="Send the NDA template to bob@example.com (Bob Smith).",
    config=types.GenerateContentConfig(
        tools=[types.Tool(function_declarations=[send_signing_request])]
    ),
)

call = result.function_calls[0]
print(execute(call.args))
```

</Tab>

<Tab title="CLI de Gemini vía MCP">

Firma dispone de dos servidores MCP. Ambos funcionan con la CLI de Gemini. Conectarlos convierte a Gemini en algo que puede construir integraciones con Firma y operar sobre tus datos de Firma.

- **firma-api** (`https://mcp.firma.dev/mcp`) da a Gemini acceso directo a la API de Firma: 84 herramientas para solicitudes de firma, plantillas, espacios de trabajo y webhooks.
- **firma-docs** (`https://docs.firma.dev/mcp`) da a Gemini acceso a la documentación completa de Firma para que genere código de integración preciso en lugar de adivinar.

### Configuración de la CLI de Gemini

Añade ambos servidores a la configuración de la CLI de Gemini. Usa `~/.gemini/settings.json` para una configuración global, o `.gemini/settings.json` en tu proyecto para acceso con ámbito de proyecto:

```json
{
  "mcpServers": {
    "firma-api": {
      "httpUrl": "https://mcp.firma.dev/mcp"
    },
    "firma-docs": {
      "httpUrl": "https://docs.firma.dev/mcp"
    }
  }
}
```

Reinicia la CLI. En el primer uso de una herramienta de Firma, Gemini te guiará para iniciar sesión con tu cuenta de Firma mediante OAuth.

Una vez conectado, puedes pedir a la CLI cosas como:

- "Lista mis solicitudes de firma pendientes en el espacio de trabajo Sales"
- "Crea una solicitud de firma a partir de la plantilla de NDA y envíala a jane@example.com"
- "Añade el endpoint create-and-send de Firma a este proyecto de Next.js. Usa los docs de Firma para la forma exacta de la petición."

<Warning>
  Gemini Code Assist para particulares se retirará el 18 de junio de 2026. Los usuarios del plan individual deben migrar a Gemini Code Assist Standard/Enterprise o a la CLI de Gemini. La configuración MCP funciona igual en todos los planes.
</Warning>

<Note>
  Cuándo usar cada servidor: `firma-api` es para hacer cosas (enviar solicitudes de firma, gestionar plantillas). `firma-docs` es para construir cosas (generar código de integración con detalles precisos de la API). La mayoría de los desarrolladores querrán tener ambos conectados.
</Note>

### Prompts que funcionan bien con el agente

**Generar una integración de backend completa:**

```text
Using the Firma API docs, create an Express route that:
1. Accepts a name, template_id, signer email, and signer name
2. Uses the create-and-send endpoint to send a signing request
3. Returns the signing request ID and the signer's signing link
Store the API key in process.env.FIRMA_API_KEY.
```

**Añadir firma integrada a una aplicación React:**

```text
Using the Firma docs, add an embedded signing view to this React app.
After a signing request is created, render the signer's signing_link
in an iframe.
```

**Configurar webhooks:**

```text
Using the Firma docs, create a webhook handler that listens for
signing_request.completed events and updates the contract status
in our Postgres database.
```

Referenciar explícitamente "los docs de Firma" indica al agente que consulte el servidor MCP `firma-docs` antes de escribir código. Sin eso, puede recurrir al conocimiento general y omitir detalles de endpoints o autenticación.

</Tab>

</Tabs>

## Integración de webhooks

Para hacer un seguimiento de los eventos de firma en tiempo real, registra un webhook de Firma que apunte a un endpoint de tu aplicación. Aquí tienes un gestor mínimo para `signing_request.completed`:

```javascript
// e.g. app/api/firma-webhook/route.ts
import { NextRequest, NextResponse } from "next/server";

export async function POST(req: NextRequest) {
  const { type, data } = await req.json();

  if (type === "signing_request.completed") {
    const signingRequestId = data.signing_request.id;
    // Update your DB, trigger downstream automation, etc.
  }

  return NextResponse.json({ received: true });
}
```

En el panel de Firma, en **Settings > Webhooks**, registra la URL de tu endpoint. Firma envía eventos para todos los cambios de estado importantes. Consulta la [guía de webhooks](/guides/webhooks) para ver la lista completa de eventos y la verificación de firma.

<Warning>
  Verifica siempre la firma del webhook usando tu secreto de firma de webhook de Firma en producción. Consulta la [guía de webhooks](/guides/webhooks) para conocer los detalles de implementación.
</Warning>

## Firma integrada

Para aplicaciones donde los firmantes completan los documentos dentro de tu UI en lugar de abrir una página alojada en Firma, la respuesta de `create-and-send` incluye `first_signer.id` (el `signing_request_user_id`) y un `first_signer.signing_link` listo para usar. Carga la URL del firmante en un iframe:

```html
<iframe
  src="https://app.firma.dev/signing/{signing_request_user_id}"
  style="width:100%;height:900px;border:0;"
  allow="camera;microphone;clipboard-write"
  title="Document Signing"
></iframe>
```

Consulta la [guía de firma integrada](/guides/embeddable-signing) para conocer la configuración completa, incluyendo las mejores prácticas de seguridad.

## Consejos

- **Usa function calling para agentes en tiempo de ejecución y MCP en tiempo de construcción.** Function calling es lo que tu aplicación desplegada usa para enviar documentos realmente. Los servidores MCP son lo que tú y la CLI de Gemini usáis para construir esa integración.
- **Pasa `template_id` como argumento de la herramienta, no como cadena libre.** Las plantillas son la forma más segura de restringir lo que el modelo puede enviar. Deja que el modelo elija un ID de plantilla de una lista corta y con nombre que tú controles.
- **Valida antes de enviar.** Para documentos más críticos, ramifica en un paso de confirmación. Usa `POST /signing-requests` para crear un borrador, muéstraselo al usuario y llama a `POST /signing-requests/{id}/send` solo después de que confirme.
- **Espacios de trabajo para aplicaciones multitenant.** Si estás construyendo un producto SaaS sobre Gemini, dale a cada cliente final su propio espacio de trabajo de Firma para que las plantillas y el uso queden aislados.

## Próximos pasos

- [Autenticación de la API](/guides/authentication): claves de API y ámbito por espacio de trabajo
- [Guía de webhooks](/guides/webhooks): tipos de eventos, cargas útiles y verificación de firma
- [Firma integrada](/guides/embeddable-signing): experiencia de firma dentro de la aplicación
- [Creación de espacios de trabajo](/guides/creating-workspaces): configuraciones multitenant para aplicaciones SaaS
- [Integración con MCP](/guides/mcp): referencia completa del servidor MCP con las 84 herramientas
- [Guía completa de configuración](/guides/complete-setup-guide): recorrido de integración de Firma de extremo a extremo
- [Referencia de la API](/api-reference): documentación completa de los endpoints
